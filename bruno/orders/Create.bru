meta {
  name: Create
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/orders
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{accessToken}}
}

body:json {
  {
    "items": [
      { "productId": "REPLACE_WITH_PRODUCT_ID", "quantity": 1 }
    ]
  }
}
